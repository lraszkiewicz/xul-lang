void printIntArithmetics(int a, int b) {
  print(a, b, a + b, a - b, a * b, a / b, a % b);
}

int main() {
  for (int a in 4 to 6) {
    for (int b in 4 downto 2) {
      printIntArithmetics(a, b);
    }
  }
  print("aaa" + "" + "bbb");
  int a = 2;
  print(a);
  a++;
  print(a);
  a--;
  print(a);
  print(-1, (-1) + 2);
  bool b = true;
  print(b, !b);
  return 0;
}
