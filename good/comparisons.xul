void printIntComparisons(int a, int b) {
  print(a < b, a <= b, a > b, a >= b, a == b, a != b);
}

void printStringComparisons(string a, string b) {
  print(a < b, a <= b, a > b, a >= b, a == b, a != b);
}

int main() {
  int b = 3;
  for (int a in 2 to 4) {
    printIntComparisons(a, b);
  }
  printStringComparisons("aab", "aac");
  printStringComparisons("aaa", "aaa");
  printStringComparisons("aac", "aab");
  print(true == true, true == false, true != true);
  return 0;
}
